<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Registry>
    <AddedComponents>
        <Component name="alpha-executor" version="${project.version}"/>
        <Component name="alpha-repo" version="${project.version}"/>
        <Component name="alpha-console-gui" version="${project.version}"/>
        <Component name="web-application" version="${project.version}"/>
        <Component name="web-server" version="${project.version}"/>
    </AddedComponents>
    <ResolvedComponents>
        <Component name="alpha-executor" version="${project.version}"/>
        <Component name="alpha-repo" version="${project.version}"/>
    </ResolvedComponents>
</Registry>
